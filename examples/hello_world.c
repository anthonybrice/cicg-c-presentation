//Includes the code from the standard (std for short) 
//library needed to do input and output (io)
#include <stdio.h>

//The main function
//In C, the main fucntion returns an error code, which
//is an int. 0 means 'no error'.
int main() {
	
	//Print (formatted) output to the terminal
	printf("Hello, world!\n");
	
	return 0;
}
